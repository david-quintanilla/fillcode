

/**
 * ---------------------------------------------------------------------------
 * Defined globals variables and config styles
 * ---------------------------------------------------------------------------
 */
platonic_nonsense = #141A26;
black_navy        = #474D59;
watercolour       = #83A4C3;
almost_white      = #FAFAFA;
pineapple         = #EEA910;


/**
 * ---------------------------------------------------------------------------
 * Keyframes
 * ---------------------------------------------------------------------------
 */
@keyframes opacity-transition
	for e in 0..10
		{10% * e}
			opacity (e/10)

/**
 * ---------------------------------------------------------------------------
 * Global class config theme
 * ---------------------------------------------------------------------------
 */
body
	font-family 'Ubuntu', sans-serif
	font-size 16px
	margin 0
	padding 0
.show
	display block !important
	animation-name opacity-transition
	animation-duration 1s
	